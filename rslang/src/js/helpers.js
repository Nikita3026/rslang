// import Router from './router/router';
// import { routes } from './router/routes';

// const router = new Router(routes);
// export const startRouting = () => {
//   // const linkElement = document.querySelectorAll('a');
//   // linkElement.forEach((it) => it.addEventListener('click', (event) => {
// //   event.preventDefault();
// //   event.stopPropagation();
// //   const { dataset: { link } } = event.target;
// //   const paramsLink = link.split('_');
// //   if (paramsLink.length > 1) {
// //     router.loadRoute(paramsLink[0], paramsLink[1]);
// //   } else router.loadRoute(link);
//   // }));
//   // const urlSegments = path.split('/').filter((it) => it.length);
//   router.loadInitialRoute();
// };

// export default {};
